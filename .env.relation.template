# Re:lation API 設定テンプレート
# このファイルをコピーして .env に設定を追加してください

# ========== Re:lation API 設定 ==========

# Re:lation アクセストークン
# 取得方法: Re:lation管理画面 > 設定 > APIトークン で発行
RELATION_ACCESS_TOKEN=your_access_token_here

# Re:lation サブドメイン
# 例: https://your-company.relationapp.jp の場合は "your-company"
RELATION_SUBDOMAIN=your_subdomain_here

# メッセージボックスID (数値)
# 取得方法: Re:lation管理画面のURLまたはAPI レスポンスから確認
# 例: https://your-company.relationapp.jp/message_box/123 の場合は "123"
RELATION_MESSAGE_BOX_ID=123

# APIタイムアウト (秒) - オプション
RELATION_TIMEOUT=30

# ========== 設定値の確認方法 ==========

# 1. アクセストークンの取得:
#    - Re:lationにログイン
#    - 画面右上の設定アイコン → システム設定
#    - 左メニュー「APIトークン」
#    - 「新規作成」ボタンでトークン発行
#    - 権限: 最低限「チケット読み取り」「チケット作成」が必要

# 2. サブドメインの確認:
#    - ブラウザのアドレスバーで確認
#    - https://[ここがサブドメイン].relationapp.jp

# 3. メッセージボックスIDの確認:
#    - Re:lation管理画面 → 受信箱設定
#    - URLの末尾の数値を確認
#    - または API で /message_boxes を取得して確認

# ========== トラブルシューティング ==========

# 404 エラーが発生する場合:
# 1. サブドメインが正しいか確認
# 2. メッセージボックスIDが数値で正しいか確認  
# 3. APIトークンが有効か確認

# 401 エラーが発生する場合:
# 1. APIトークンが正しく設定されているか確認
# 2. トークンの有効期限が切れていないか確認
# 3. トークンの権限が適切か確認

# 403 エラーが発生する場合:  
# 1. レート制限 (60回/分) を超過していないか確認
# 2. IPアドレス制限が設定されていないか確認
# 3. メッセージボックスへのアクセス権限があるか確認